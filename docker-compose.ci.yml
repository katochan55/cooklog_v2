version: '3'
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.ci
    container_name: cooklog_v2_app
    # command: bash -c "rm -f tmp/pids/server.pid && bundle exec rails s -p 3000 -b '0.0.0.0'"
    command: bundle exec rails s -b '0.0.0.0'
    # volumes:
    #   - .:/myapp
    ports:
      - "3001:3000"
    depends_on:
      - db
    networks:
      - default

  db:
    image: postgres
    # volumes:
    #   - ./tmp/db:/var/lib/postgresql/data
