<% list = List.find_by(dish_id: @dish.id) %>
<%= link_to "/lists/#{list.id}/destroy",
            method: :delete,
            id: 'unlist',
            remote: true do %>
  <% current_user == @dish.user ? "作る予定リストから削除" : "食べたい！リクエストを取り消す" %>
<% end %>
